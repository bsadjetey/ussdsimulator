import { NewVersionCheckerService } from './new-version-checker-service';

describe('NewVersionCheckerService', () => {
  it('should create an instance', () => {
    expect(new NewVersionCheckerService()).toBeTruthy();
  });
});
